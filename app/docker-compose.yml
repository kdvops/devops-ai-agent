services:
  agent:
    build: .
    ports: ["8080:8080"]
    environment:
      WORKSPACE_ROOT: /workspace
      KUBERNETES_READ_ONLY: "true"
      ALLOWED_NAMESPACES: default
    volumes:
      - ./workspace:/workspace
